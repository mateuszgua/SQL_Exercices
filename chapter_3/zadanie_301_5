SELECT CHANNEL, PRODUCT_ID, AVG(SALES_AMOUNT) AS AVG_SALES 
FROM SALES
WHERE CHANNEL = 'dealership' AND CHANNEL IS NOT NULL 
GROUP BY GROUPING SETS (
	(CHANNEL),
	(PRODUCT_ID),
	(CHANNEL, PRODUCT_ID)
)
ORDER BY CHANNEL, PRODUCT_ID;