SELECT *
FROM CUSTOMERS C
FULL JOIN (
	SELECT S.CUSTOMER_ID, P.* 
	FROM SALES S
	LEFT JOIN
	PRODUCTS P ON S.PRODUCT_ID = P.PRODUCT_ID ) AS PR
	ON C.CUSTOMER_ID = PR.CUSTOMER_ID
ORDER BY C.CUSTOMER_ID;
